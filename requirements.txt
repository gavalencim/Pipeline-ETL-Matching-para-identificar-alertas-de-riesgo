# ── Core ETL ──────────────────────────────────────────────────────────────────
requests==2.31.0          # HTTP con soporte de sesiones y timeouts
tenacity==8.2.3           # Reintentos con backoff exponencial
lxml==5.1.0               # Parser XML de alta performance
beautifulsoup4==4.12.3    # HTML scraping (World Bank)
chardet==5.2.0            # Detección de encoding en CSVs

# ── Procesamiento de datos ─────────────────────────────────────────────────────
pandas==2.2.1             # Transformaciones tabulares
numpy==1.26.4             # Operaciones vectorizadas

# ── Almacenamiento analítico ──────────────────────────────────────────────────
duckdb==0.10.1            # Base analítica local sin servidor

# ── Matching ──────────────────────────────────────────────────────────────────
rapidfuzz==3.6.1          # Jaro-Winkler y Levenshtein optimizados en C
Unidecode==1.3.8          # Normalización de caracteres Unicode/tildes

# ── Calidad y validación ──────────────────────────────────────────────────────
pydantic==2.6.4           # Validación de schema con tipos estrictos
great-expectations==0.18.12  # Reglas de calidad declarativas

# ── Utilidades ────────────────────────────────────────────────────────────────
python-dotenv==1.0.1      # Variables de entorno por ambiente
click==8.1.7              # CLI para run_pipeline.py --env local
rich==13.7.1              # Logging visual en terminal
structlog==24.1.0         # Logging estructurado (JSON) para producción
tqdm==4.66.2              # Progress bars en procesos batch
pycountry==23.12.11       # Validación y normalización ISO 3166

# ── Síntesis de datos ─────────────────────────────────────────────────────────
faker==24.2.0             # Generación de personas/empresas ficticias

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.1.1
pytest-cov==5.0.0
responses==0.25.0         # Mock de llamadas HTTP en tests
